# 课立方 编码计划书（Demo V1）

> 配套文档：`课立方-项目计划书(1).md`。本文件只讲**怎么把它写出来**，不讲商业论证。
> 版本：Demo V1 · 撰写日期：2026-09-14

---

## 〇、一条总原则

**这是用来演示的，不是用来上线的。**

判断任何一段代码该不该写，只问一个问题：**评委看演示的时候，它会不会出现在屏幕上？**

- 会 → 认真写，写扎实。
- 不会 → 不写。哪怕"正规系统都该有"。

这条原则决定了下面一切取舍。demo 的竞争力在**能演示出多少真实业务**，不在架构有多完整。一个能跑通"点一次签到，课时、工资、收入三处同时变"的丑系统，赢过一个架构漂亮但演示不动的系统。

---

## 一、交付范围

七个模块全部打通，但每块只做到"演示级可用"。

### 1.1 做什么

| 模块 | Demo 做 |
| --- | --- |
| 智能排课 | 四类课表、硬约束冲突检测、贪心构造 + 局部搜索、单次调课增量重排 + 影响面展示 |
| 课程签到 | 教师端点名、学生端 6 位口令签到、五种状态自动核销课时、课时账户与流水、请假审批 + 补课推荐 |
| 工时薪酬 | 计酬规则配置（分档单价 / 特殊课型 / 奖励扣款）、工资单生成、点进工资项能看到对应签到记录 |
| 财会报表 | 签到即确认收入、收支明细、预收账款台账、课时结转表、教师成本表、班级毛利表 |
| 内容托管 | 四级分类、上传、版本与回滚、可见范围、按课表推送本节内容 |
| 交互式课件 | JSON 课件、9 类互动组件播放器、结构化编辑器、课堂互动统计回流 |
| 电子作业 | 组卷、布置、提交、客观题自动判分、主观题批改、错题本、三视角学情报告 |

### 1.2 明确不做（不是遗漏，是决策）

**基础设施类 —— 这些是 demo 里最典型的浪费：**

- 多租户隔离 —— demo 只有一家机构，不做 `org_id`、不做 SQL 拦截器
- Flyway 数据库迁移 —— 用 `schema.sql` + `data.sql`
- Spring Security / JWT / 刷新令牌 —— 用一个拦截器 + HttpSession
- 审计日志 AOP、操作留痕
- Redis / Caffeine —— 动态口令用 `ConcurrentHashMap` + 过期时间
- Docker、CI/CD、Nginx、HTTPS、域名
- 虚拟线程、连接池调优、接口限流、缓存层
- 单元测试覆盖要求（只保留第五节列出的几条一致性断言）

**功能类：**

- 微信小程序 —— 家长端/学生端走同一套 Vue3 的手机版路由
- 短信 / 微信模板消息 —— 站内通知 + 页面轮询
- 扫码签到、地理位置校验、人脸识别
- PPT/PDF 导入、课件自由画布布局、课件模板沉淀
- 语音/视频作业提交、手写批注、分层布置
- Excel / PDF 导出 —— 报表页面能看就行，不做下载
- 教师薪酬申诉流程、自定义结算周期（只做按月）
- 现金流预测预警、课耗异常预警、软约束权重自学习

---

## 二、技术栈

后端 4 个依赖，前端 6 个。**多一个是多的。**

### 后端

```xml
spring-boot-starter-web          <!-- 含 Jackson、Tomcat -->
mybatis-plus-spring-boot3-starter
mysql-connector-j
lombok
```

| 组件 | 选型 | 理由 |
| --- | --- | --- |
| 框架 | Spring Boot 3.2 + Java 21 | 指定 |
| 持久层 | MyBatis-Plus | **比手写 MyBatis 少写代码**，`BaseMapper` 白送 CRUD，`LambdaQueryWrapper` 白送条件构造。它是净减法 |
| 数据库 | MySQL 8.0 | 指定 |
| 建表 | `schema.sql` + `data.sql` | Spring Boot 原生：`spring.sql.init.mode=always`，启动自动执行。零依赖、零配置 |
| 登录 | `HandlerInterceptor` + `HttpSession` | 约 40 行。不引 Spring Security |
| 文件 | 本地磁盘 `./data/uploads` | 一个工具类 |

**不用 Flyway**：demo 阶段改表结构是常态，直接改 `schema.sql` 然后 drop 库重建，比写迁移脚本快得多。

**不用 Spring Security**：它带来的配置面（filter chain、密码编码器、认证管理器、CORS、CSRF）远超 demo 所需。

### 前端

`vue` `vue-router` `pinia` `axios` `element-plus` `echarts` + `vite` + `typescript`

| 组件 | 用途 |
| --- | --- |
| Element Plus | 后台表格/表单/弹窗开箱即用，省掉绝大部分 CSS |
| ECharts | 经营看板与学情报告的图表 —— **demo 里图表的说服力远高于数字表格** |
| Pinia | 存当前登录用户 |

---

## 三、工程结构

```
课立方/
├── README.md
├── .gitignore
├── 课立方-项目计划书(1).md
├── 编码计划书.md                    ← 本文件
├── backend/
│   ├── .env / .env.example
│   ├── pom.xml
│   └── src/main/
│       ├── java/com/kelifang/
│       │   ├── KelifangApplication.java
│       │   ├── common/              # 统一返回、异常处理、登录拦截器、当前用户、文件工具
│       │   ├── auth/                # 登录、登出、当前用户
│       │   ├── basedata/            # 学生、教师、课程、班级、教室、校区
│       │   ├── schedule/            # 排课 + engine 子包（求解器）
│       │   ├── attendance/          # 签到、核销、课时账户、请假补课
│       │   ├── salary/              # 薪酬规则、工时、工资单
│       │   ├── finance/             # 资金流水、报表
│       │   ├── content/             # 内容资产
│       │   ├── courseware/          # 课件与互动结果
│       │   └── homework/            # 作业、错题本、学情
│       └── resources/
│           ├── application.yml
│           ├── schema.sql           ← 建表
│           └── data.sql             ← 种子数据
└── frontend/
    ├── .env / .env.example
    ├── package.json
    └── src/
        ├── api/                     # 每模块一个 ts
        ├── router/
        ├── stores/
        ├── layouts/                 # 桌面端布局 + 手机端布局
        └── views/                   # 按模块分目录
```

一个 Spring Boot 进程，按业务分包。**没有 mapper XML** —— MyBatis-Plus 的 `LambdaQueryWrapper` 能表达全部查询，报表的聚合查询用 `@Select` 注解写在 Mapper 接口上。

---

## 四、配置

### 4.1 后端

`backend/src/main/resources/application.yml` —— 全文就这么长：

```yaml
server:
  port: 8080

spring:
  config:
    import: "optional:file:./.env[.properties]"   # 唯一的"额外配置"，一行
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:kelifang}?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always              # 启动自动执行 schema.sql + data.sql
      continue-on-error: false
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted

kelifang:
  storage-path: ${STORAGE_PATH:./data/uploads}
```

关键点：`spring.config.import` 把 `.env` 当 properties 文件读进来，`application.yml` 里用 `${DB_PASSWORD}` 引用。**不引任何第三方库。**

`optional:` 前缀保证没有 `.env` 时也能启动（走默认值），不会启动失败。

### 4.2 前端

Vite **原生**读 `.env`，零配置代码：

```
VITE_API_BASE_URL=/api
```

用相对路径 `/api`，配合 Vite 的 proxy 转发到 8080 —— 这样**完全不用处理 CORS**：

```ts
// vite.config.ts
server: {
  port: 5173,
  proxy: { '/api': { target: 'http://localhost:8080', changeOrigin: true } }
}
```

同理，session cookie 走同源，也不会有跨域携带问题。

### 4.3 `.env.example`

`backend/.env.example`：

```properties
# 复制为 .env 后按本机情况修改。.env 不提交到 git。
DB_HOST=localhost
DB_PORT=3306
DB_NAME=kelifang
DB_USERNAME=root
DB_PASSWORD=your_password_here
STORAGE_PATH=./data/uploads
```

`frontend/.env.example`：

```
VITE_API_BASE_URL=/api
```

### 4.4 `.gitignore`

```
.env
.env.local

backend/target/
frontend/node_modules/
frontend/dist/
data/

.idea/
.vscode/
*.iml
```

### 4.5 启动步骤（写进 README）

```bash
# 1. 建库
mysql -uroot -p -e "CREATE DATABASE kelifang DEFAULT CHARSET utf8mb4;"

# 2. 后端
cd backend && cp .env.example .env      # 改 DB_PASSWORD
mvn spring-boot:run                     # 自动建表 + 灌种子数据 → :8080

# 3. 前端
cd frontend && cp .env.example .env
npm install && npm run dev              # → :5173
```

**注意 `data.sql` 会往表里灌数据，每次重启都会重复插入。** demo 阶段的处理办法：`schema.sql` 里每个 `CREATE TABLE` 用 `DROP TABLE IF EXISTS` 打头，重启即重建。种子数据永远干净，不用手工清库。

### 4.6 测试账号

`data.sql` 里建好 5 个角色的账号，密码统一 `123456`，登录页给出快捷登录按钮：

| 角色 | 账号 | 演示时看到什么 |
| --- | --- | --- |
| 校长 | `principal` | 全部菜单 + 经营看板 |
| 教务 | `academic` | 排课、学员、签到异常 |
| 教师 | `teacher01` | 我的课表、签到、课件、作业、我的工资单 |
| 学生 | `student01` | 我的课表、作业、错题本、学情报告 |
| 家长 | `parent01` | 孩子的课表、课时余额、作业、到离校通知 |

---

## 五、数据模型

24 张表。相比上线版砍掉了：`org`、`prepaid_ledger`、`schedule_job`、`schedule_conflict`、`charge_order` 与 `refund_order` 合并、`content_version` 直接并入 `content_asset`。

**除 `sys_user`、`campus` 外，所有业务表都带 `created_at`；核心流水表（`lesson_transaction`、`fund_transaction`）只增不改。**

### 5.1 账号与组织

| 表 | 说明 | 关键字段 |
| --- | --- | --- |
| `sys_user` | 五类角色共用账号 | `id`, `username`, `password`, `real_name`, `role`, `phone` |
| `campus` | 校区 | `id`, `name`, `address` |

### 5.2 基础数据

| 表 | 说明 | 关键字段 |
| --- | --- | --- |
| `teacher` | 教师 | `id`, `user_id`, `name`, `subject`, `campus_id`, `salary_type`(HOURLY/BASE_HOURLY/MONTHLY), `base_salary` |
| `student` | 学生 | `id`, `user_id`, `name`, `grade`, `campus_id`, `parent_user_id` |
| `course` | 课程 | `id`, `name`, `subject`, `grade`, `duration_minutes`, `weekly_times`, `tags`(JSON：`["TRIAL"]`/`["MAKEUP"]` 等) |
| `classroom` | 教室 | `id`, `campus_id`, `name`, `capacity` |
| `clazz` | 班级 | `id`, `name`, `course_id`, `teacher_id`, `classroom_id`, `campus_id`, `capacity` |
| `class_student` | 班级名单 | `class_id`, `student_id` |
| `teacher_availability` | 教师可用时段 | `teacher_id`, `weekday`, `start_time`, `end_time` |
| `student_constraint` | 学生可排/不可排时段 | `student_id`, `type`(AVAILABLE/UNAVAILABLE), `weekday`, `start_time`, `end_time` |

### 5.3 排课

| 表 | 说明 | 关键字段 |
| --- | --- | --- |
| `schedule` | **排课结果，四类课表都从这张表派生** | `id`, `class_id`, `teacher_id`, `classroom_id`, `lesson_date`, `start_time`, `end_time`, `status`(PLANNED/CANCELLED/DONE), `locked` |

`locked = 1` 是增量重排的锚点：调一节课时，只对 `locked = 0` 且落在影响范围内的行重新求解，其余原样保留。不建任务表 —— 求解同步执行，结果直接返回。

### 5.4 签到与课时

| 表 | 说明 | 关键字段 |
| --- | --- | --- |
| `attendance` | 签到记录（**全链路凭证起点**） | `id`, `schedule_id`, `student_id`, `status`(PRESENT/LATE/EARLY_LEAVE/LEAVE/ABSENT), `sign_method`, `sign_time`, `consumed_hours` |
| `lesson_account` | 课时账户 | `id`, `student_id`, `course_id`, `total_hours`, `consumed_hours`, `remaining_hours` |
| `lesson_transaction` | 课时流水（**只增不改**） | `id`, `student_id`, `type`(RECHARGE/CONSUME/GIFT/ADJUST), `hours`(带正负), `balance_after`, `ref_id`, `remark` |
| `leave_request` | 请假与补课 | `id`, `student_id`, `schedule_id`, `reason`, `status`, `deduct_hours`, `makeup_schedule_id` |

`lesson_account.remaining_hours` 是冗余字段，由 `lesson_transaction` 累加得出。**任何写操作必须在同一事务里同时写流水和更新余额，余额永远不允许被直接覆盖。** 这是计划书"流水式记账、全部可追溯"的强制实现，也是第 7 条验收标准要断言的。

### 5.5 薪酬

| 表 | 说明 | 关键字段 |
| --- | --- | --- |
| `salary_rule` | 计酬规则 | `id`, `teacher_id`(null 为默认), `tier_config`(JSON 分档), `special_rate`(JSON), `bonus_config`(JSON), `deduct_config`(JSON) |
| `workhour_record` | 工时明细（**签到自动生成**） | `id`, `teacher_id`, `schedule_id`, `attendance_id`, `work_date`, `student_count`, `rate`, `amount` |
| `payslip` | 工资单 | `id`, `teacher_id`, `period`(yyyy-MM), `total_amount`, `detail`(JSON 快照) |

`workhour_record` 由签到核销时同事务写入 —— 这正是计划书场景一里"课没上但工资照发"的技术解法：没有有效签到就不可能有工时记录。

### 5.6 财会

| 表 | 说明 | 关键字段 |
| --- | --- | --- |
| `fund_transaction` | 资金流水（**只增不改**），预收与收入确认都是这张表的行为 | `id`, `student_id`, `type`(PRE_RECEIVE/RECEIVE_CONFIRM/REFUND/EXPENSE), `amount`, `direction`(IN/OUT), `ref_id`, `occur_date`, `remark` |

**只有这一张表**，`prepaid_ledger` 和 `charge_order` 全部砍掉，需要时从流水聚合出来。

**收入确认的实现在哪里** —— `AttendanceService.consume()` 核销课时的同一个事务里：

```
课时单价 = 该学员该课程已收费总额 / 已购课时数
本次确认收入 = 课时单价 × 本次核销课时
→ 写 fund_transaction(PRE_RECEIVE, OUT, 金额)   // 冲减预收账款
→ 写 fund_transaction(RECEIVE_CONFIRM, IN, 金额) // 确认当期收入
```

这是计划书"按课时消耗确认收入"的落地，也是整个 demo 最有说服力的一处代码。

### 5.7 内容与课件

| 表 | 说明 | 关键字段 |
| --- | --- | --- |
| `content_asset` | 内容资产（版本直接内联） | `id`, `name`, `subject`, `grade`, `course_id`, `knowledge_point`, `type`, `file_path`, `version_no`, `scope`(ALL/SUBJECT_GROUP/PRIVATE), `owner_id` |
| `courseware` | 交互式课件 | `id`, `name`, `schema_json`(LONGTEXT) |
| `courseware_record` | 互动结果回流 | `id`, `courseware_id`, `schedule_id`, `student_id`, `component_id`, `answer`(JSON), `correct` |

### 5.8 作业与学情

| 表 | 说明 | 关键字段 |
| --- | --- | --- |
| `question` | 题库 | `id`, `subject`, `grade`, `knowledge_point`, `type`(SINGLE/MULTI/BLANK/SUBJECTIVE), `stem`, `options`(JSON), `answer`(JSON), `score` |
| `homework` | 作业 | `id`, `class_id`, `name`, `question_ids`(JSON), `total_score`, `due_time`, `publisher_id` |
| `homework_submission` | 提交 | `id`, `homework_id`, `student_id`, `submit_time`, `attachment_paths`(JSON), `score`, `status` |
| `submission_answer` | 逐题作答 | `id`, `submission_id`, `question_id`, `answer`(JSON), `score`, `correct`, `comment` |
| `wrong_question` | 错题本 | `id`, `student_id`, `question_id`, `knowledge_point`, `wrong_count`, `last_wrong_time` |

### 5.9 通知

| 表 | 说明 | 关键字段 |
| --- | --- | --- |
| `notification` | 站内通知 | `id`, `receiver_id`, `title`, `content`, `ref_type`, `ref_id`, `is_read`, `created_at` |

---

## 六、七个模块的实现要点

### 6.1 智能排课（demo 的技术制高点）

**求解器自己写，不引 OR-Tools。** 理由：问题规模是"20 教师 / 120 节课每周"，贪心构造在这个规模是秒级的；OR-Tools 在 Windows 上带 native 库，为一个 demo 增加环境复杂度不值。计划书第五章本来就是"约束规划求解器与自研启发式算法相结合"，demo 只做自研那一半。

`schedule/engine/` 三个类：

1. **`ConstraintValidator`** —— 硬约束校验。教师/学生/教室时段碰撞、容量超限、连堂连续性、合规时段。返回冲突清单而非抛异常，**demo 里必须能看到"为什么不给排"**。

2. **`GreedyScheduler`** —— 构造阶段。按"可排时段最少的班级优先"排序，逐个放入第一个合法三元组。

3. **`LocalSearchOptimizer`** —— 优化阶段。三种邻域动作（同班两节课交换时段、换教室、换教师），接受目标函数改进的解，10 秒上限或若干轮无改进即停。

目标函数（软约束，权重为常量）：

```
score = 5 × 学生时间意向满足数
      - 2 × 教师周课时量方差
      - 1 × 教室空置时段数
```

**demo 的收敛点**：软约束只做这三项。学生"家长接送等待时间最短"砍掉 —— 它需要真实的地理与家庭数据，demo 里灌不出来，做出来也是假数据演假效果。

**增量重排** `reschedule(scheduleId, newSlot)`：

1. 改动目标行，跑 `ConstraintValidator`。通过就直接成功（毫秒级）。
2. 不通过 → 算出受影响集（同教师/同学生/同教室的其他行）。
3. 只对受影响集里 `locked = 0` 的行重新求解，其余原样保留。
4. 返回方案 + 影响面（涉及哪些班级、教师、学生），前端弹窗让教务确认后才落库。

**四类课表是同一个接口的四个筛选视角，不建四张表。**

### 6.2 课程签到与课时核销（demo 的心脏）

- **教师端点名**：打开某节课签到页 → 学生名单 → 点状态按钮 → 一次批量提交。
- **学生端口令签到**：教师端生成 6 位数字存进 `ConcurrentHashMap`（5 分钟过期），学生输入后写签到。防的是最粗一层的代签，不追求强校验。
- **五种状态的扣减规则**写死在常量类里（demo 不做机构自定义配置）：出勤 1.0、迟到 1.0、早退 0.5、请假 0、旷课 1.0。

- **核销链路** —— 一个 `@Transactional` 方法，**这是整个系统的心脏**：

```java
AttendanceService.consume(scheduleId, studentId, status)
  ├─ 1. 写 attendance.consumed_hours
  ├─ 2. LessonAccountService.consume()  → lesson_transaction(CONSUME, -x) + 更新余额
  ├─ 3. 写 workhour_record（教师侧，按 schedule 的 teacher_id）
  ├─ 4. FinanceService.confirmRevenue() → fund_transaction 冲预收 + 确认收入
  └─ 5. 推家长通知
```

第 2–4 步在同一事务内，任一步失败整体回滚。**实现成本极低（就是顺序调用四个 service），但这是计划书里"一站式"从 PPT 变成代码的唯一地方**，也是演示的高潮。

- **补课推荐**：请假审批通过后，复用 `ConstraintValidator` 在教师空闲时段与学生可接受时段之间求交，返回前 3 个候选。

### 6.3 工时薪酬

- `SalaryRuleEngine` 按教师逐条算工资项。`tier_config` 是 JSON：`{"SMALL": [{"max": 6, "rate": 60}, {"max": 12, "rate": 75}], "LARGE": 90}`。
- 计酬时读 `workhour_record.student_count` 落档，读 `course.tags` 识别试听/补课/代课换单价。
- 满勤奖判定：查该教师当月 `attendance` 有无 ABSENT/LATE。续班奖 demo 里砍掉。
- **工资单生成时冻结一份 `detail` JSON 快照**，之后改规则不影响历史工资单。
- **演示的关键动作**：从工资单项点进去，能看到这一笔对应哪节课、哪条签到记录。这是计划书"解决工资算错了说不清"的直接证据。

### 6.4 财会报表

报表全部是单表或两表 JOIN 的 `GROUP BY`，用 MyBatis 的 `@Select` 注解写在 Mapper 接口上，**不写 XML**。

做 5 张：收支明细、预收账款台账（从 `fund_transaction` 聚合）、课时消耗与结转表、教师成本表、班级毛利表。

**收入确认是这张表的输入源**，不是独立算的 —— 演示时点一次签到，报表数字当场变，这个效果比任何架构图都有用。

**内建的一条合规校验**：录收费时校验"时间跨度 ≤ 3 个月且课时数 ≤ 60"，超了直接拒绝。计划书第五章的预收费监管要求，落在代码里只有几行，但演示时是个很好的差异化点。

### 6.5 内容托管

上传落 `./data/uploads/{uuid}.{ext}`，写 `content_asset`。改一次 `version_no + 1`，回滚就是把 `version_no` 指回旧值并重新上传旧文件（demo 不做文件级版本存储，只演示版本号流转与界面表现）。

**按课表推送**：教师打开某节课时，前端按该节课的 `course_id` 查 `content_asset` 直接列出可用内容 —— 这是"内容自动托管"里最容易演示、实现成本最低的一点。

### 6.6 交互式课件

课件 JSON：

```json
{
  "version": 1,
  "pages": [
    {
      "id": "p1",
      "title": "第一页",
      "components": [
        { "id": "c1", "type": "SINGLE_CHOICE",
          "props": { "stem": "…", "options": ["A","B"], "answer": 0 } }
      ]
    }
  ]
}
```

- **播放器**：`componentMap[type]` 注册表 → Vue 组件。9 类组件各写一个 `<XxxPlayer :props>` 小文件。
- **编辑器**：三栏 —— 左页面列表、中当前页组件列表、右属性面板。组件按顺序纵向排列，用原生 HTML5 `draggable` 调顺序。

**V1 明确不做自由画布绝对定位** —— 这是把课件编辑器从 6 周工作量压到 1 周的唯一关键决策。教师拖拽组件、填属性，出来的是一页一页的纵向流式课件，视觉上朴素但**功能完整可演示**。

- **互动结果回流**：学生端作答写 `courseware_record`，教师端轮询拿统计（参与率、正确率），生成报告的同时把错题写进 `wrong_question`（与作业模块共用错题本）。

### 6.7 电子化作业

- 组卷：从 `question` 表按学科/年级/知识点筛选，手工录入也写 `question`。
- 自动判分：按 `type` 分派策略。多选严格匹配；填空做 trim + 大小写归一 + 多答案容错。
- 主观题批改：教师逐题填分与评语。
- 错题本：判错即 upsert `wrong_question`，`wrong_count + 1`；同知识点出题时优先推送 `wrong_count` 高的。
- 学情报告：三份 SQL 聚合，学生视角（个人正确率 + 知识点雷达）、家长视角（完成率 + 趋势）、教师视角（全班知识点热力图）。**全部用 ECharts 画出来 —— demo 里图表的存在感远高于表格。**

---

## 七、登录与权限

### 7.1 登录（约 40 行）

```
POST /api/auth/login   { username, password } → 写 session 的 "currentUser"
POST /api/auth/logout
GET  /api/auth/me      → 当前用户信息
```

`LoginInterceptor` 拦 `/api/**`，session 里没有 `currentUser` 就返回 `{code: 401}`。放行 `/api/auth/login`。

**不引 Spring Security**：demo 只有 5 个测试账号，它带来的配置面远超所需。

### 7.2 五类角色的数据可见范围

| 角色 | 看到什么 |
| --- | --- |
| 校长 principal | 全部 |
| 教务 academic | 全部，但无薪酬菜单 |
| 教师 teacher | 我的课表、我的班、我的工资单 |
| 学生 student | 我的课表、我的作业、我的错题本 |
| 家长 parent | 我孩子的课表、课时余额、作业 |

**实现方式**：后端在 Service 层按当前用户强制过滤（教师只能查 `teacher_id = 自己`），前端同时按角色隐藏菜单。

**demo 阶段说明白**：前端隐藏菜单是体验，后端过滤才是边界。两边都做，但后端那一道不能省 —— 演示时如果评委用教师账号直接敲 URL 拿到别的老师的工资单，那是硬伤。

### 7.3 不做的事

- 不做权限点/按钮级权限（`@PreAuthorize` 那一套）
- 不做审计日志
- 不做密码加密（种子数据明文 `123456`）—— **但要在 README 里写明这是 demo 限定**

---

## 八、迭代计划

6 周，五批。每批结束时必须是**能跑起来演的状态**，不允许出现"三个模块同时半成品"。

| 批次 | 周 | 内容 | 结束时的演示 |
| --- | --- | --- | --- |
| 一 | 1 | 前后端骨架、`.env`、`schema.sql` + `data.sql`、登录 + 拦截器、前端布局与角色菜单、基础数据 CRUD（学生/教师/课程/班级/教室/校区） | 五个账号分别登录，看到不同菜单，基础数据可增删改查 |
| 二 | 2–3 | **排课引擎**（Validator + 贪心 + 局部搜索）、四类课表页面、调课 + 增量重排 + 影响面弹窗 | 点一下出四张课表；调一节课，系统给方案和影响面 |
| 三 | 4 | **签到 → 课时 → 工时 → 收入全链路**、课时账户与流水页、请假审批与补课推荐、工资单生成与溯源 | **点一次签到，课时、工资、收入三处同时变** |
| 四 | 5 | 财会 5 张报表 + 图表、内容托管（上传/版本/可见范围/按课表推送） | 报表页看到签到产生的真实数字 |
| 五 | 6 | 交互式课件（播放器 + 编辑器 + 互动统计）、电子作业（组卷/判分/批改/错题本/学情报告）、联调与演示打磨 | 完整演示动线走通 |

**唯一的强依赖**：批次二必须等求解器写完，后端和前端才能接。建议第 1 周就并行开始 `engine` 包开发，不要等基础数据做完。

**批次三是整个 demo 的验收线。** 如果时间不够，砍批次五（课件/作业），保住批次三 —— "签到一次，三处联动"是这家产品的全部说服力所在，课件和作业只是加分项。

---

## 九、验收标准

| # | 判据 |
| --- | --- |
| 1 | 全新机器上，建库 + `cp .env.example .env` 改密码 + 两条启动命令，5 分钟内能登录 |
| 2 | `git log --all -- backend/.env frontend/.env` 无任何输出 |
| 3 | 构造 3 个必冲突班级，系统报出冲突原因，而不是崩溃或给出非法课表 |
| 4 | 20 教师 / 120 节课规模，全量求解 ≤ 30 秒（计划书指标） |
| 5 | 单次调课出方案 ≤ 5 秒，且 `locked = 1` 的行数据未被改动 |
| 6 | 从任一工资单项可点进到对应的 `attendance` 记录 |
| 7 | **连续 20 次签到/请假/补课操作后，`lesson_account.remaining_hours` 恒等于该账户全部 `lesson_transaction.hours` 之和** |
| 8 | **任一学员的预收账款余额 = 其全部 `fund_transaction` 中预收类流水的净额** |
| 9 | **签到核销后，预收减少额 = 确认收入增加额 = 本次消耗课时 × 课时单价** |
| 10 | 教师账号直接请求其他教师的工资单接口，被后端拒绝 |
| 11 | 提交跨度 4 个月或 61 课时的收费单，被拒绝并返回明确原因 |
| 12 | 教师端新建课件 → 加 3 类互动组件 → 保存 → 学生端作答 → 教师端看到正确率统计 |

第 7、8、9 条是**一致性断言**，建议直接写成 JUnit 测试。它们不是"测试覆盖率"，它们是"一体化数据底座"这个卖点在代码层面的证据 —— 破了就说明整个产品叙事不成立。

第 1、2 条是给评委看的工程素养，成本极低，别省。

---

## 十、演示动线（demo 的最终目的）

**这一节才是这份计划书的存在理由。** 前面所有技术决策都是为了这 12 分钟能顺畅走完。

1. **校长登录** → 经营看板，数字是种子数据 + 已有流水算出来的
2. **切教务** → 排课页 → 点"智能排课" → 30 秒内出四张课表 → 切到教师视角看同一批数据的另一种呈现
3. **调一节课** → 拖到新时段 → 系统弹出冲突与影响面 → 确认 → 看到未受影响的课表纹丝不动
4. **切教师** → 打开今天第一节课 → 点名 5 个学生（含 1 个迟到、1 个请假）→ 提交
5. **切家长** → 立刻看到孩子的课时余额变了、收到了到校通知
6. **回校长的经营看板** → 刚才那 5 次签到的课时消耗、教师工时、确认收入**已经在数字里了**
7. **切教师 → 我的工资单** → 刚才那节课已经进了本月工时 → 点进去能看到签到记录
8. **切教务 → 内容托管** → 打开某节课，系统已经自动列出这节课该用的课件
9. **切教师 → 课件** → 现场做一份含 3 类互动组件的课件 → 播放 → 学生端作答 → 看到正确率
10. **切学生 → 作业** → 做一份作业 → 客观题即时判分 → 错题自动进错题本 → 学情报告雷达图变了

**第 4–6 步是整个演示的高潮**：一次点名，三个角色的屏幕同时发生真实变化。**这三步必须打磨到零卡顿、零等待**，比任何其他功能都重要。

**演示准备**：种子数据要设计得"看起来像真的"—— 300 名学生、15 位教师、8 个班级、一个学期的课表、若干条历史流水。数字太干净（比如全是 100）反而假。

---

## 十一、已知取舍

| 取舍 | 代价 | demo 阶段可接受吗 |
| --- | --- | --- |
| 明文密码 | 无安全性 | 可接受，README 必须写明是 demo 限定 |
| 无多租户 | 只能演示一家机构 | 可接受 —— 计划书的产品定位本来就是"只服务单家机构" |
| `data.sql` 每次重启重灌 | 演示数据无法积累 | 可接受，且演示时正好需要干净的初始状态 |
| 无 Flyway | 改表结构要 drop 库 | 可接受，demo 阶段本来就频繁改表 |
| 同步求解 | 大机构会阻塞请求 | 可接受，demo 数据规模 120 节课 |
| 课件不做自由画布 | 版面表现力弱 | 可接受，重点是互动组件能跑通 |
| 无 PPT/PDF 导入 | 存量资料要手工重建 | **演示时要主动说明这是规划中的能力**，不要假装已实现 |
| 无小程序 | 家长/学生用浏览器 H5 | **这是演示时最可能被问到的点**，要主动说明小程序在路线图中 |
| 无短信/模板消息 | 通知只在站内 | 同上，主动说明 |
| 软约束权重固定 | 计划书说的"数据飞轮"在这版不成立 | **演示话术不能说成已实现的自学习**，只能说"权重可配置" |
| 无导出 | 报表只能看 | 可接受 |

**最后三条要特别提醒**：计划书第八章把"权重自学习"、"到离校实时通知"、"微信小程序"都写成了产品能力。**demo 演不出来就不要在演示时说成已实现** —— 评委顺着问一句就穿了。正确说法是"已规划 / 第二阶段接入"。

---

## 十二、开工前要确认的两件事

1. **薪酬口径**：`salary_rule` 的 JSON 结构要覆盖多少种算法？这是返工重灾区。建议拿一家真实机构的工资表反向推导字段，而不是从理论设计。**批次三开始前必须拿到这张表。**

2. **课时单价怎么算**：`收费单金额 / 课时数` 这个简单除法在"赠送课时 + 优惠"时会产生歧义（赠送课时单价是否为 0？结转按哪个单价？）。**建议规则**：赠送课时不计入分母，单独记 `GIFT` 流水、单价为 0。这个规则要产品确认后写进常量类。

---

*本计划书与 `课立方-项目计划书(1).md` 的差异只在一处：demo 的交付范围比计划书的路线图更薄，且刻意跳过了几乎全部基础设施。所有被砍掉的东西都在第一节与第十一节列明 —— 不是遗漏，是为了把 6 周的人力全部压在能被看见的地方。*
