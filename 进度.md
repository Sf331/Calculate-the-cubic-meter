# 开发进度与交接

> 这份文件记录**当前做到哪了、下一步做什么、有哪些坑**。每次收工更新一次。
> 技术方案本身看 `编码计划书.md`，不要在这份文件里重复。

最后更新：2026-09-19

---

## 一句话状态

**批次一至四已完成并验证通过，数据库为 H2 内存库。** 批次五的**交互式课件、电子作业、错题本和基础学情报告最小闭环**也已完成。当前剩余的是浏览器人工验收，以及学生端口令签到、请假审批、退费等明确不阻塞主线的补充项。

**2026-09-19：界面整体换成了姊妹项目 `Calculate-the-cubic-meter-main-2` 的风格**（详见下一节）。

代码规模：后端 115 个 Java 文件（另有 4 个验收测试文件），前端 37 个。

---

## 界面：从 `-main-2` 移植的「简洁专业 · 双色」（2026-09-19）

### 为什么做

原规则是「追求最简单、能用的界面，不做任何花哨的设计」+「字体统一用宋体」。
用户要求改成姊妹项目 `Calculate-the-cubic-meter-main-2` 的界面 ——
那个项目单独做了五轮 UI 收敛（参考件 `stylekit.top/zh/styles/data-dense`），
但只实现了 5 个视图；本项目功能齐（15 个视图）而界面是裸的。**这次是把风格移植过来，不是复制过去。**

**原来的「宋体 + 不加样式」规则已作废**，`CLAUDE.md` 里那一节整个换掉了。
`frontend/src/styles/index.css` 从 19 行变成 800+ 行（token 体系 + Element Plus 变量映射 + `.k-*` 工具类）。

### 怎么做的：合并，不是覆盖

两个项目的代码是**分叉**的，不是新旧版本。移植时按三层走，每一层都保住本项目的功能：

| 层 | 做法 |
| --- | --- |
| 基础 | `styles/index.css`、`layouts/`、`views/login`、`views/Placeholder`、`NotFound`、`components/` 直接照搬 |
| 共有视图 | 以 `-main-2` 的版式为准，逐个把本项目独有的功能补回去（见下表） |
| 本项目独有 | 按新风格重写：签到、薪酬、财务、内容、课件、作业、错题、学情、课时账户、经营看板 |

**特有功能一个都没丢**，补回去的：

- `CrudTable` 加了 `-main-2` 没有的 `file` 字段类型（上传 + `uploadUrl`）、`readonly`、`actionWidth` 和 `#actions` 插槽
- `crud.ts` 的 `CrudField` 同时有 `minWidth`（新，默认）和 `width`（旧，只给长度恒定的窄列，如版本号）
- 学员管理的「收费开课」弹窗、基础数据的「课时单价」字段
- `router/menus.ts` 全套菜单 + 新增 `MenuItem.short`（手机端标签栏短名）和 `canEditSchedule(role)`

依赖**没动**：本项目是 vite ^5.2 / echarts ^5.5，`-main-2` 是 vite ^8.3 / echarts ^6.1。
这套风格是纯 CSS 变量 + Element Plus，不需要新依赖，所以一个 `package.json` 字段都没改。

### 第二轮：字号与排版重做（2026-09-19，同日晚些时候）

移植完用户看完说「**优化一下字体大小与排版，目前这个版本太丑了**」。
他勾的问题类型是「字号层级没拉开 + 整体偏大偏松 + 中文排版细节不对 + 版式结构问题」，
正文字号选「**降到 17px**」，最丑的页面勾了「经营看板 + 排课与课表 + 表格类页面 + 登录页 / 侧栏顶栏」——
四类全中，所以是一次全面检修，不是局部微调。

**字号阶从 `15/16/18/22/26/32` 改成 `14/15/17/21/25/30`（等比，档距 1.13～1.24）。**
旧的那套两头都坏：小端三档只差 1～2px（表头、按钮、正文同屏像同一个大小），
大端每档跳 4～6px（正文和 KPI 之间像隔了两级）。**"丑"的主要来源就在这。**

中文排版上修了四处，都是**照抄拉丁文规范照出来的毛病**：

| 位置 | 原来 | 改成 | 为什么 |
| --- | --- | --- | --- |
| 表头 | `letter-spacing: 0.06em` | 去掉 | 汉字是满格方块，拉开后「班级容量」散成四块、左右边缘参差 |
| 页面标题 | `letter-spacing: -0.01em` | 去掉 | 负字距是给拉丁字母收紧的，汉字再收紧会挤在一起 |
| 空态文案 | `max-width: 42ch` | `max-width: 360px` | `ch` 是拉丁单位，一个汉字≈2ch，手机上把「继续保持」劈成两行 |
| 数字 | `font-family: --font-mono` | `tabular-nums` | 换字族让数字和中文变成两套字形，一行里像贴了两种字 |

密度：表格行内边距上下各 12px → 8px（**行高 51px → 43px**），按钮字号对齐输入框（`--text-xs` → `--text-sm`），
状态标签反而**调大**到 `--text-xs`（它是"要读的状态"，不是"扫一眼的表头"）。

版式上加了 `.k-page--fill` / `.k-page__fill` 一档（见 `CLAUDE.md`），用在四处：
经营看板（图表写死 360px，1440×900 下卡片底下空 180px）、排课与课表、
交互课件、电子作业（后两个是主从两栏 `align-items: start`，底下空 700px）。
课表空列的「无课」也从垂直居中改成顶部对齐 —— 居中的两个字会飘在 400px 高的格子正中间，
跟隔壁从顶上排下来的课程块不在同一条视线上。

顺带修的：登录页「课」字叠了两次（方块 logo 在上、标题在下，都是「课」），改成同一行。

**这一套同步回了 `-main-2` 的 `index.css`**（两个项目共用同一个样式层，只改一边下次移植又会漂回来）。
只同步了共享的样式层，**页面代码没动** —— `.k-page--fill`、登录页的改法是本项目的增量，
`-main-2` 自己的排课页早就有一份等效的 scoped 写法。

### 移植时抓到的四个真 bug

都是**渲染出来看**才发现的，`npm run build` 一个都报不出来（这个项目的 build 只跑 `vite build`，不做类型检查）：

| 症状 | 真因 | 修法 |
| --- | --- | --- |
| KPI 数字墙最后一行留一格灰底，像坏了一格 | `repeat(auto-fit, minmax(200px,1fr))` 装 8 格时排成 5+3，余位露出分隔线底色 | 列数写死 4/2/1（8 和 4 都能整除），`.k-kpi` 自己当卡片，不再套 `.k-card` |
| **三张表的「操作」列被挤出可视区** | 各列 min-width 单独看都合理，加起来（1230 / 1580 / 1230）超过内容区 1186px，el-table 横向滚动 | 按预算重压列宽。**页面级 `scrollWidth` 查不出来**，得量 `.el-table__body-wrapper` 的 `scrollWidth` |
| 空表空态文字撑着 60px 行距、还折成两行 | `.el-table__empty-text` 上写死了 `line-height:60px` + `width:50%`，`.k-empty` 整套继承 | `index.css` 里把这两个值还原成 `100%` / `1.5` |
| 手机上「还没有错题，继续保持」断成「保 / 持」 | `max-width: 42ch` —— `ch` 是拉丁文单位，中文一个字 ≈ 2ch，桌面刚好、手机差一个字符 | 改成 `max-width: 360px` + `text-wrap: balance` |

前两个是**本项目特有**的：`.k-kpi` 是这次新写的工具类，`-main-2` 里没有；列宽超预算则是因为本项目的表更宽
（内容托管 10 列，`-main-2` 最宽的表只有 5 列）。后两个（`line-height:60px`、`42ch`）**`-main-2` 里一模一样地存在** ——
那是 Element Plus 和这份 css 本身的问题，不是移植带来的，两边都已修（见 `-main-2/进度.md`）。

### 验证方式

`npm run build` **不做类型检查**，所以只信渲染结果。用 Node 22 自带的 `WebSocket` 直接驱动 CDP
（不装 puppeteer），登录后逐页截图 + 量计算样式：

- **桌面端**：11 个页面 × 1440×900。顶栏 21px / 表头 14px / 单元格 17px / 按钮 17px / 菜单 15px / KPI 30px，
  行高 43～46px，页面无横向溢出。
- **全部表格**：10 个页面 × 每个页签，量 `.el-table__body-wrapper` 的 `scrollWidth`。**最终全部 0 溢出。**
- **手机端**：学生角色（走 `MobileLayout`）× 390×844，底部标签栏 5 格、无横向溢出；「更多」弹层正常。
- 顺带排了一次课（40 节）确认课表格子、签到表格在有数据时也正常。
- **「填满没有」也量**：探针算 `内容区底部 − 最后一个顶层块底部`，>24px 记为没填满。
  注意排除 `display:none` 的 `el-dialog` 遮罩 —— 它也是 `.k-page` 的子元素，
  不排掉的话"最后一个子元素"永远是它，这个指标恒等于页高。

验证用的是**隔离端口**（前端 5200 → 后端 18081），没碰项目自己的 5173 / 8080。

---

## ⚠️ 待验证：换 H2 内存库的遗留风险（2026-09-16）

数据库已从 MySQL 8 换成 **H2 内存库**（`jdbc:h2:mem:kelifang;MODE=MySQL`），不需要装任何数据库。
改动细节和语法差异见 `编码计划书.md` 4.1 节。**下面这些点没有验证过，碰的时候要当心。**

### 未验证项：11 个 JSON→TEXT 列，所属模块还没开发

MySQL 的 `JSON` 列在 H2 里必须写成 `TEXT`（原因见 4.1）。`schema.sql` 里**全部 12 个 JSON 列已改**，但其中**只有 1 个跑通过真实读写**：

| 表.列 | 所属模块 | 状态 |
| --- | --- | --- |
| `course.tags` | basedata | ✅ **已验证** —— 增删改查 + `["TRIAL"]` 解析都正常 |
| `salary_rule.tier_config` | salary | ❌ **未验证** —— 模块未开发 |
| `salary_rule.special_rate` | salary | ❌ **未验证** |
| `salary_rule.bonus_config` | salary | ❌ **未验证** |
| `salary_rule.deduct_config` | salary | ❌ **未验证** |
| `payslip.detail` | salary | ✅ **已验证** —— 但**没走 TypeHandler**，实体声明成 `String`、Service 里用 ObjectMapper 显式读写（原因见下方「重要约定」）。写入/读回/逐项求和都对得上 |
| `courseware_record.answer` | courseware | ❌ **未验证** |
| `question.options` | homework | ❌ **未验证** |
| `question.answer` | homework | ❌ **未验证** |
| `homework.question_ids` | homework | ❌ **未验证** |
| `homework_submission.attachment_paths` | homework | ❌ **未验证** |
| `submission_answer.answer` | homework | ❌ **未验证** |

**为什么未验证**：`content`、`courseware`、`homework` 三个模块目前只有 `.gitkeep`，实体和 Mapper 都不存在，没有读写路径可以跑。（`attendance`、`finance`、`salary` 三个模块已经实现，但它们的表里没有 JSON 列。）

**开发到这些模块时怎么确认**：实体字段上挂 `@TableField(typeHandler = JacksonTypeHandler.class)` 并给实体加 `@TableName(value="...", autoResultMap = true)`（照抄 `Course.java`），然后读写一次这个字段。
**如果报 `MismatchedInputException: Cannot deserialize value of type ArrayList from String value`**，说明该列在 H2 下仍有二次编码问题，需要单独处理 —— 别以为是业务代码写错了。

### 已验证：前端 + 端到端链路

前端不直连数据库（只走 `/api`），换库不影响它。已实测：`npm run build` 通过（1694 模块）；
起前后端后经 Vite 代理（:5173）登录、查 6 张基础表、跑排课引擎，全部正常。

### 其他遗留

- `编码计划书.md` 里与 H2 相关的部分已同步更新；但那份文档的**数据模型章节引用的字段类型仍是 MySQL 口径**（如 `JSON`），以 `schema.sql` 为准。
- `schema.sql` 里还有 11 个**复核过但未实际查询**的普通索引（`idx_parent`、`idx_receiver` 等）。它们只是索引，不影响正确性。

### 计划书已按"只做基本实现"精简（2026-09-16）

**612 行 → 501 行。** 原则：demo 只要求功能跑通，砍掉一切算法优化、可配置化、格式规范。

| 章节 | 变化 | 砍掉了什么 |
| --- | --- | --- |
| 六 实现要点 | 115 → 52 行 | 局部搜索优化器、软约束目标函数、预收费合规校验、课件 JSON schema、9 类组件枚举、三视角学情报告 |
| 九 验收标准 | 12 → 6 条 | 未开发模块的条目、规模性能指标 |
| 十 演示动线 | 10 → 6 步 | 后 4 步并成一句"时间够就继续演" |
| 四 配置 | 127 → 87 行 | `.env` 那一节、`.gitignore` 整节 |
| 一 交付范围 | — | 修掉与第六节打架的"局部搜索""9 类组件" |

**一、二、三、七、八、十一、十二这 7 节一行没动** —— 它们本来就是"做什么、不做什么、什么顺序做"。

**注意**：第五节的 28 张表清单**完整保留**（含「关键字段」列）。它是 `schema.sql` 的契约，后面四个批次照着它写代码，别嫌长。

---

## 已完成

| 步骤 | 内容 | 关键产出 |
| --- | --- | --- |
| 0 | 工程框架 + 端到端验证 | `pom.xml`、`application.yml`、`schema.sql`（28 表）、`data.sql`（种子）、登录与拦截器 |
| 1 | 校区 + 教室 CRUD | `common/BaseCrudController`（泛型基类）、`common/IdEntity`、前端 `components/CrudTable.vue`（字段配置驱动的通用 CRUD 表格） |
| 2 | 教师 CRUD | `basedata/entity/Teacher.java` 四件套 |
| 3 | 课程 + 班级 CRUD | `Course.java`（`tags` 用 `JacksonTypeHandler` 映射 JSON 列）、`Clazz.java`；`CrudTable` 加 multiselect / defaultValue |
| 4 | 学员管理 | `Student`、`ClassStudent`、`ClazzService.studentsByClass()`、`AuthService.listByRole()`；前端「学生」+「班级名单」双 tab（`el-transfer`） |
| 5 | 排课引擎 | `schedule/engine/`：`TeachingGrid`、`Booking`、`ClassTask`、`ScheduleContext`、`ConstraintValidator`、`GreedyScheduler`、`Conflict`；四类课表查询接口 |
| 6 | 课表页面 + 手动调课 | `views/schedule/index.vue`（7 列周视图）、`PUT /api/schedule/{id}/move`；**负载均衡修复** |
| 7 | 增量重排 + 影响面 | `engine/IncrementalRescheduler`、`engine/Placement`、`POST /api/schedule/{id}/reschedule`；前端「预览方案 → 确认调整」两步弹窗 |
| 8 | 批次三·签到 → 课时 → 工时 → 收入（后端） | `attendance` / `salary` / `finance` 三个模块；`AttendanceService.consume()` 四步同事务；`teacher.hourly_rate`；种子灌课时账户与预收 |
| 9 | 批次三·签到页 + 课时账户页 | `views/attendance/`、`views/lesson-account/`；`GET /api/attendance/lessons` 按 teacher_id 后端过滤 |
| 10 | 批次三·工时薪酬 + 财会报表 + 经营看板 | `Payslip`/`PayslipService`、`ReportService`（5 张报表）、`DashboardService`；三个页面，看板用 ECharts |
| 11 | 批次三·收费开课入口 | `POST /api/lesson-account/recharge`；`CrudTable` 加 `actions` 插槽；学员管理页每行「收费开课」弹窗 |
| 12 | 验收对账改成 JUnit | `src/test/java/com/kelifang/acceptance/`：`AcceptanceApi`（HTTP 客户端）+ `AcceptanceReconcileTest`（16 项断言）+ `AcceptanceResetTest`（重置）；引入 `spring-boot-starter-test` |
| 13 | 批次四·内容托管 | `content` 模块四件套 + `POST /api/content/upload`；`CrudTable` 加 `file` 字段类型；`views/content/`；`AcceptanceContentTest`（9 项断言）。**接通了之前一直没人用的 `StorageUtil` + `/uploads/**` 链路** |

### 已提交

```
3b70e44 修复bug
9b07d07 经营看板              ← 步骤 10 后半
201cf77 签到流水              ← 步骤 10 前半
2f5727e 更新
8a45b5b 数据库无感使用        ← H2 换库 + 删 .env 配置 + 文档同步（14 个文件）
a052298 去掉mysql数据库文件…   ← 空壳提交，见下
c72e905 进度
81efd5f 增量重排和形象面       ← 步骤 7
cb3e493 智能排课              ← 步骤 6（含负载均衡修复）
f0d025e 排课引擎：…            ← 步骤 5
9a9bddb 基础数据即curd开发     ← 步骤 1–4
1910554 框架初始化
735f786 初始化
```

**批次一、二、三（步骤 1–10）都已提交并推送**（GitHub + Gitee 双远程）。工作区未提交的是步骤 11（收费开课）和 `WorkhourService.java` 的注释改动。

> ⚠️ **`a052298` 是个空壳提交**：提交信息写的是"H2 改造"，但内容只有删除 `.claude/settings.json` 一个文件。原因是被改动的那 14 个文件从没被 `git add` 过，提交时只有暂存区里的 settings.json 删除。**教训：在 VSCode 里提交前，确认 "Changes" 栏是真的空了**，只看 "Staged Changes" 会漏掉未暂存的改动。

### `.claude/settings.json` 已脱离 git 跟踪

这个文件里嵌着明文密码（`mysql -uroot -p123456`）和一堆一次性 curl 权限，属于会话噪音。已处理：

1. 删掉 4 条 mysql 权限（数据库没了，权限全废）
2. `git rm --cached .claude/settings.json` 让它不再被跟踪（**文件仍在磁盘上，本地权限配置没丢**）
3. `.gitignore` 末尾加 `/.claude/settings.json`

**注意**：`.gitignore` 对**已被跟踪**的文件无效，必须配 `git rm --cached` 才生效。

---

## 下一步：批次三（进行中）

按 `编码计划书.md` 第八节，批次三是**签到 → 课时 → 工时 → 收入全链路**，这是整个 demo 最有说服力的一段。

### ✅ 已完成：后端全链路（步骤 8）

`attendance` / `salary` / `finance` 三个模块，接口如下：

| 接口 | 作用 |
| --- | --- |
| `GET /api/attendance/lessons?from=&to=` | 可点名的课次列表。**教师只拿得到自己的课**，教务校长拿全部 |
| `GET /api/attendance/{scheduleId}` | 点名页数据：课次信息 + 全班名单（含已点状态、剩余课时、课时单价） |
| `POST /api/attendance/{scheduleId}` | 提交点名。一个请求落库四样：签到 + 课时流水 + 教师工时 + 收入确认 |
| `GET /api/lesson-account?studentId=` | 课时账户（学生看自己、家长看孩子、校长教务看全部） |
| `GET /api/lesson-account/{id}/transaction` | 某账户的全部课时流水 |
| `GET /api/finance/transaction?studentId=` | 资金流水。`ref_id` 指回 `attendance`，能溯源到具体哪次点名 |

**核销的四步**（`AttendanceService.consume()`，`@Transactional`）：

```
1. 写 attendance（扣课时由状态算，见 AttendanceRule）
2. 扣课时：lesson_transaction(CONSUME, -x) + 更新 lesson_account 余额
3. 写 workhour_record（一节课一条，不按人数重复计酬）
4. 确认收入：fund_transaction 冲预收(OUT) + 记收入(IN)，金额相等
```

**实施时定下的口径**（计划书第十二节要求"实施前明确"的三条）：

- **固定课时单价存在 `teacher.hourly_rate`**，一张表一个价。没用 `salary_rule` 表（它的 4 个 JSON 分档列按精简后的口径全用不上）
- **教学课时 = 1 / 节**。只要这节课有 ≥1 个 `PRESENT/LATE/EARLY_LEAVE`，就给老师记 1 个教学课时；金额 = `hourly_rate × 1`。**不是**把学生扣减课时加总（计划书禁止）
- **学生扣课时**：出勤 1.0 / 迟到 1.0 / 早退 0.5 / 请假 0 / 旷课 1.0，写死在 `AttendanceRule`
- **课时单价** = 该学员该课程 `PRE_RECEIVE(IN)` 之和 ÷ `lesson_account.total_hours`。靠 `fund_transaction.ref_id = lesson_account.id` 关联，**没有新增金额列**
- **赠送课时（GIFT）本次完全不碰**，种子只灌充值。口径未确认前不把建议写进代码

### ✅ 已完成：签到页 + 课时账户页（步骤 9）

| 页面 | 路径 | 谁能进 | 做什么 |
| --- | --- | --- | --- |
| 签到管理 | `views/attendance/` | 校长 / 教务 / 教师 | 选日期范围 → 列出可点名的课次 → 点「点名」开弹窗 → 名单上每行选出勤/迟到/早退/请假/旷课 → 提交 → 弹出"扣了多少课时、确认多少收入、记了多少工时" |
| 课时账户 | `views/lesson-account/` | 学生 / 家长 | 列出课时账户（已购/已消耗/剩余/单价）→ 点「流水」看每一条课时变动 |

两个页面都进了 `router/menus.ts`，不再落到 `Placeholder`。

**演示时要留意的点**：

- 点名弹窗里**默认所有人按"出勤"**，老师只需要改动少数几个，少点几下（`views/attendance/index.vue` 的 `open()`）
- **没有课时账户的学生不给默认值**（那一行「剩余课时」显示"无账户"）。他被带上整批会让整批回滚，所以默认不提交，由老师在界面上决定怎么办
- 已点过名的学生那一行是**禁用**的，重开弹窗也不会重复提交
- 教师看到的课次由后端 `/api/attendance/lessons` 按 `teacher_id` 过滤，前端改不了范围
- 核销失败时报错**带学生姓名**（"张三 还没有这门课的课时账户"），一批人里卡在谁身上直接看得出来

### ✅ 已完成：工时薪酬 + 财会 5 张报表 + 经营看板（步骤 10）

| 页面 | 路径 | 谁能进 | 内容 |
| --- | --- | --- | --- |
| 经营看板 | `views/dashboard/` | 校长 | 本月确认收入 / 课时消耗 / 教师成本 / 预收余额 + 出勤人次、学员数等；ECharts 折线图画本月逐日收入与课时消耗 |
| 工时薪酬 | `views/salary/` | 校长 / 教师 | 「工资单」tab：按月生成工资单、点「明细」看逐项快照、再点「看签到」回到那节课的签到名单；「工时明细」tab 按天列每节课的工时 |
| 财会报表 | `views/finance/` | 校长 | 5 个 tab：收支明细 / 预收台账 / 课时结转 / 教师成本 / 班级毛利 |

接口：

| 接口 | 作用 |
| --- | --- |
| `GET /api/salary/workhour?from=&to=` | 工时明细。教师只拿得到自己的 |
| `POST /api/salary/payslip/generate` | 生成某月工资单，按教师分组把逐条工时冻进 `detail` 快照 |
| `GET /api/salary/payslip?period=` `GET /api/salary/payslip/{id}` | 工资单列表 / 详情。教师查别人的单返回 403（**验收标准第 6 条**） |
| `GET /api/finance/transaction?from=&to=&type=` | 收支明细（带学生姓名） |
| `GET /api/finance/report/{pre-receive,lesson-carry,teacher-cost,class-profit}?period=` | 另外 4 张报表 |
| `GET /api/dashboard?period=` | 经营看板。非校长 403 |

**实施时定下的口径**：

- **工资单不含底薪**，就是 Σ 课时单价 × 有效教学课时（`teacher.base_salary` 闲置，不参与计算）
- **`payslip.detail` 不用 MyBatis-Plus 的 `JacksonTypeHandler`**，见下方「踩过的坑」
- **口径统一按"课次日期"归属**（收入、课时消耗都算在课上的那天，不是点鼠标那天），所以点完名回看板数字当场就变
- **预收台账是累计数**，只认截止月不认起始月；余额 = 预收(IN) − 冲减(OUT)
- **班级毛利要三跳**才对得上班级：`fund_transaction.ref_id` → `attendance` → `schedule` → `clazz`
- **财会报表和经营看板后端限校长**（和菜单一致）。注意计划书 7.2 说"教务看全部，只是没有薪酬菜单"，这里没按那条放开 —— 要放开就把 `ReportController.requirePrincipal()` 和 `DashboardController` 的判断改成校长+教务

### ✅ 已完成：收费开课入口（步骤 11）

| 位置 | 谁能用 | 做什么 |
| --- | --- | --- |
| 学员管理页「学生」tab，每行「收费开课」 | 校长 / 教务 | 选课程 + 填充值课时、收费金额、备注 → 提交后弹出新余额与单价 |

接口：

| 接口 | 作用 |
| --- | --- |
| `POST /api/lesson-account/recharge` | 收费开课。没有账户顺手开一个，返回充值后的账户状态（含新单价） |

**为什么放在学员管理页，不是课时账户页**：课时账户页只列**已有账户**，新招的学生在那页根本列不出来，
正好卡死在"最需要收费的就是没有账户的人"上。学员管理页每个学生都在，一个不漏。

**实施时定下的口径**：

- **`@Transactional` 三处同变**：课时账户余额、课时流水（`RECHARGE`）、资金流水（预收 `IN`）。
  收了钱没加课时、或加了课时没记账，都是演示现场会被问住的数据
- **余额只由流水累加**，不直接覆盖 —— `remaining_hours == Σ lesson_transaction.hours` 这条不变式充值路径也守住了
- **资金流水的 `ref_id` 指向 `lesson_account.id`**，和种子数据一个写法。所以**充值不用改任何报表代码**：
  课时单价（`FundService.paidTotals`）、预收台账、收支明细、经营看板的预收余额全都自动带上这笔钱
- **金额必须 > 0**，不开口子做 0 元赠送 —— `GIFT` 口径还没确认，不给它偷偷开一条路径
- **只有校长和教务能收费**，学生 / 家长 / 教师一律 403（后端拦，不是藏菜单）。教务能收是因为钱在教务手上，和「学员管理」菜单的角色一致
- **课程可以选和学生所在班级不一致的**（没有做限制），弹窗里写了一句提示。选错了点不了名，报"还没有这门课的课时账户"

**验收**：见下面的「步骤 12」—— 充值的断言已经并进 `AcceptanceReconcileTest` 的用例 4、5、9，
不再单独用脚本。

### ✅ 已完成：验收对账写成 JUnit（步骤 12）

`backend/src/test/java/com/kelifang/acceptance/`：

| 文件 | 作用 |
| --- | --- |
| `AcceptanceApi.java` | 验收用的 HTTP 客户端。走真实端口 + `CookieManager` 会话。**不是测试类**，是三个文件里唯一被复用的那个 |
| `AcceptanceReconcileTest.java` | 验收对账，16 个用例，`@Order` 钉死顺序 |
| `AcceptanceResetTest.java` | 重置验收。删掉验收造的临时学生、把班级名单里的孤儿行摘掉；`resetAll()` 给对账类收尾用 |

```bash
cd backend && mvn -B test        # 16 个用例
```

**为什么改成 JUnit**：计划书第九节末尾写着"第 3、4 条是一致性断言，建议直接写成 JUnit 测试"。
之前那条"连 `spring-boot-starter-test` 都不加、验证走 `验收对账.py`"的约定到此为止 ——
**但加了 `spring-boot-starter-test`（test scope）是唯一新增的依赖**，业务代码零依赖不变。

**边界仍然是"不信服务自报"**：`AcceptanceApi` 只发 HTTP，测试里**没有注入任何 Service**。
同一个 JVM 里直调 Service 会把这条界线抹掉（共用 Spring 上下文和事务），
所以走 HTTP 不只是"更像真实调用"，它就是这条断言的成立前提。

**比老 Python 脚本强的地方**：`@SpringBootTest` 跑在自己 JVM 的 `jdbc:h2:mem:kelifang` 里
（H2 内存库按 JVM 隔离），**不碰你 `mvn spring-boot:run` 演示的那个库**。
老脚本是真会往演示库里塞「充值验收生」的，每次跑完都得重启后端。

**前置**：种子数据里**没有课次** —— 课表是 `POST /api/schedule/generate` 排出来的。
所以 `@BeforeAll` 先排 4 周课（起点 `2026-09-14`），`@Order(0)` 断言一节冲突都没报。
**别把课次当成种子数据写死条数**，它取决于引擎。

**覆盖**（按执行顺序）：

| # | 断言 |
| --- | --- |
| 0 | 排课：`conflictCount == 0` 且 `placedCount > 0` |
| 1 | **验收3** 每个账户 `remaining_hours` == 该账户全部 `lesson_transaction.hours` 之和（全库逐账户） |
| 2 | 账户 `total_hours` / `consumed_hours` == 流水里 `RECHARGE` / `CONSUME` 的合计 |
| 3 | 课时单价 == 该账户已收预收总额 / 已购课时（**单价自己从资金流水重算，不读接口返回的那个数**） |
| 4–5 | 收费开课：新学生从无账户到开课；同账户追加充值单价不变 |
| 6 | 一次点名：`consumedHours` / `confirmedAmount` / `workhourAmount` 都动，且三份账仍然平 |
| 7 | **防重复核销**：同节课同学生再点一次 `code=400`，且报错说得出原因 |
| 8 | **事务回滚**：一批里混进没课时账户的学生，整批不落库（判据是这节课一个人都没签到） |
| 9 | 收费权限：学生 / 家长 / 教师 403，教务放行 |
| 10 | 家长指定别人家孩子的 `studentId`，仍然只拿得到自己孩子 |
| 11–12 | 教师只拿得到自己的课次（且是校长全量的子集）；给别人的课点名 403 |
| 13 | 学生只看到自己的课时账户 |
| 14 | **收尾**：所有写操作跑完之后，1/3/4 三条再各验一次 |

**这个测试验过它自己有用**：把 `LessonAccountService.consume()` 里的
`account.setRemainingHours(balanceAfter)` 人为改成 `balanceAfter.add(BigDecimal.ONE)`，
用例 6 和 14 当场变红，报错指名道姓（"高晨曦 / 初二物理入门班 剩余课时：期望 39.0，实际 40.0"）。
改回来了，现在 16 个全绿。

> ⚠️ **`mvn -B spring-boot:run` 不会跑这些测试**，得单独 `mvn -B test`。
> 另外测试类之间共享 Spring 上下文，所以 `AcceptanceResetTest` 那个用例和
> `AcceptanceReconcileTest` 用同一份库里 —— 两边都不该假设自己先跑。

### ✅ 已完成：内容托管（步骤 13，批次四）

| 页面 | 路径 | 谁能进 | 内容 |
| --- | --- | --- | --- |
| 内容托管 | `views/content/` | 校长 / 教务 / 教师 | 顶部选课次（不选就是全部）→ 列出该课次对应课程下的内容；增删改查 + 上传文件；每行「下载」 |

接口：

| 接口 | 作用 |
| --- | --- |
| `GET /api/content?page=&size=` | 列表（**基类那个，被覆写加了可见范围过滤**） |
| `GET /api/content/list?page=&size=&courseId=` | 表格用的列表，多一个课程筛选 = "按课表推送本节内容" |
| `POST /api/content/upload` | 上传文件，返回落盘后的相对文件名 |
| `POST/PUT/DELETE /api/content[/{id}]` | 增删改（继承基类，删除被覆写加归属校验） |

**为什么这个模块值得单独说**：`StorageUtil`（本地落盘）和 `WebConfig`（`/uploads/**` 静态映射）
从步骤 0 就写好了，`vite.config.ts` 里 `/uploads` 的代理也早就配了，
前端 `api/request.ts` 的 `upload<T>()` 也一直在 —— **但全仓没有一个 controller 或页面调用过它们**。
这个批次就是把这条铺了三个月没人走的路接通。

**实施时定下的口径**：

- **上传走两步**：先 `POST /upload` 拿相对文件名，再 `POST /content` 建资产。
  一步到位的 multipart 没法复用 `BaseCrudController.create`，得手写 `@RequestParam` 拼对象，代码更多
- **不新增"按课表推送"接口**：`ScheduleView` 本来就带 `courseId`，前端选完课次自己就知道按什么筛。
  走 `schedule → clazz → courseId` 是多余的一跳
- **`scope` 只做 `ALL` / `PRIVATE` 两档**，不做 `SUBJECT_GROUP` —— 库里没有学科组表，
  也没有教师↔科目组的数据。传 `SUBJECT_GROUP` 会被白名单拒掉
- **版本只递增，不做回滚**：改一次 `version_no + 1`，不存历史文件（计划书 5.7 写的就是"版本直接内联"）
- **`version_no` 读库自算**，不认前端回传的值 —— `PUT` 走 `updateById` 全字段覆盖，
  前端把整行拷进表单，传上来的是上一次的旧值
- **`owner_id` 存的是 `sys_user.id`，不是 teacherId**，而且由后端按登录态填，**不认请求体**
- **可见范围**：校长/教务看全部；其余角色只看 `scope='ALL'` 或 `owner_id=自己`
- **维护内容的角色限校长/教务/教师**（含上传接口）。菜单只对这三个角色显示，但**边界在后端** ——
  学生账号直接敲 URL 建不了内容、也传不了文件

**踩到的坑（两个都真踩了）**：

1. **基类的 `page(long, long)` 不能加参数**。加了就变成重载而不是覆写，两个方法映射同一个
   `GET /api/content`，Spring 启动直接 `Ambiguous mapping`。做法是**原签名覆写**（加可见范围过滤，
   不覆写的话教师能直接请求 `/api/content` 绕过去看到所有人的私藏）+ 另开 `/content/list` 接 `courseId`
2. **`or` 必须用 `and(...)` 括起来**。跟在 `courseId` 后面直接 `.or()` 会拼成
   `course_id = ? AND scope = 'ALL' OR owner_id = ?`，`AND` 结合更紧，等于
   `(course_id=? AND scope='ALL') OR owner_id=我` —— 自己挂在别的课上的私藏会被捞进来。
   **注意这个漏法拿"别人的内容"验不出来**，必须拿"自己的、别的课的"内容验（见 `AcceptanceContentTest` 用例 6）

**验收**：`AcceptanceContentTest` 9 项断言，`mvn -B test` 一起跑。
上传那条是**真的把字节从 `/uploads/` 取回来比**，不是看接口返回个文件名就当成功。
另外**这个测试也验过它自己有用**：把 `and(...or...)` 的括号拆掉，用例 6 当场变红。

**演示动线**（计划书批次四的验收口径："上传内容后，教师打开对应课表能看到内容"）：
校长登录 → 内容托管 → 建一条讲义挂到某班的课程 → 切教师 → 顶部选该课次 → 列表出现 → 点「下载」能打开。

### ✅ 已完成：批次五·交互式课件与电子作业（步骤 14）

| 模块 | 已交付 |
| --- | --- |
| 交互式课件 | `courseware` 后端、H2-safe JSON 文本读写、页面/组件结构校验、学生互动答题、教师互动统计；前端目录、新建、播放器和答题 |
| 电子作业 | 题库、作业组卷、学生提交、单选/多选/填空自动判分、教师主观题批改、提交详情 |
| 错题本 | 学生错题聚合与错误次数更新 |
| 学情报告 | 基础得分、正确率、错题和知识点统计页面 |

新增路由：`/courseware`、`/homework`、`/wrong-book`、`/report`。后端保持角色和数据范围校验，JSON 字段使用现有 H2 `TEXT` 兼容方案。后端 `mvn -q -B clean test` 与前端 `npm run build` 均已通过。

### ❌ 未做

- **学生端 6 位动态口令签到**未做（只做了教师点名）
- **请假审批 + 补课推荐**（`leave_request`）未做 —— 请假目前只在点名时选「请假」状态，不走审批流
- **退费**未做 —— `fund_transaction` 有 `REFUND` 类型、`lesson_transaction` 有 `REFUND` 类型，但没有任何入口写它们
- **报表不能导出**（计划书明确不做）
- **内容的版本回滚未做**（只递增版本号，不存历史文件）
- **`SUBJECT_GROUP` 可见范围未做**（无数据模型支撑）
- **孤儿文件不清理**：上传后取消创建、或编辑时重传，旧文件留在磁盘上。demo 可接受

**下一步建议**：进行浏览器人工验收和演示打磨。重点走通教师创建课件、学生答题、教师查看统计，以及教师布置作业、学生提交、查看错题本和学情报告的完整动线。

> 计划书第六节已按"只做基本实现"精简过（见上方「计划书已精简」）。**照精简后的版本做，不要再引入局部搜索、合规校验、9 类组件这些纸面设计。**

**薪酬口径已定（2026-09-17）**：固定课时单价 × 有效教学课时，暂不做分档/奖惩，**不再等真实工资表**。实施细节见上（`teacher.hourly_rate`）。

**赠送课时口径待确认**：`GIFT` 不入分母、单价为 0 仍是建议，不能当作已确认规则，见计划书第十二节。**代码里目前完全没有 GIFT 路径。**

**2026-09-17 本次交付**：批次五·交互式课件与电子作业（步骤 14）。新增 `courseware` / `homework` 后端模块、
前端课件、作业、错题本、学情报告页面；`mvn -q -B clean test` 与 `npm run build` 均通过。
此前批次四内容托管的 `AcceptanceContentTest` 仍纳入后端完整测试。
**接通了从步骤 0 就铺好、但一直没人用的 `StorageUtil` + `/uploads/**` + `upload()` 那条链路。**
**内容托管页、课时账户页、工时薪酬页、财会报表页、经营看板、收费弹窗的页面渲染仍未经人眼验证**（见文末）。


---

## 重要约定（别推翻）

### 数据与配置

- 数据库：**H2 内存库**（`jdbc:h2:mem:kelifang;MODE=MySQL`），不需要装任何数据库
- **没有 `.env`**。四个 `.env` / `.env.example` 已全部删除：上传目录写死在 `application.yml` 的 `kelifang.storage-path`，前端 API 前缀写死在 `request.ts` 的 `baseURL: '/api'`。**新增配置项前先问「这个值真的会变吗」，不会变就写死，别再引入 `.env`**
- **每次重启后端，整库重建**。`schema.sql` 每张表都是 `DROP TABLE IF EXISTS` 打头，`spring.sql.init.mode=always`。改表结构直接重启即可，不用手工清库；但**演示数据不会累积**
- **H2 索引名是全库唯一的**（MySQL 是表级唯一），所以 schema.sql 里重名的索引都带了表名前缀，例如 `idx_teacher_user_id`。新增索引时注意别撞名
- **JSON 列在 H2 里写成 `TEXT`**。MySQL 的 `JSON` 列 `getString()` 返回原始 JSON 文本，H2 的 `JSON` 类型返回的是二次编码的字符串，会让 `JacksonTypeHandler` 解析失败
- **MP 的 `JacksonTypeHandler` 有两个坑，别直接用在复杂类型上**（2026-09-17 踩过）：
  1. 它用的是**自带的裸 ObjectMapper**，不认 `LocalDate` 这类 Java 8 时间类型，写的时候直接报 `Java 8 date/time type not supported by default`
  2. 它按**原始类型**（`List.class`）反序列化，元素会变成 `LinkedHashMap`，泛型全丢；再往外序列化就报 `object is not an instance of declaring class`

  `course.tags`（`List<String>`）两个坑都躲得过，所以一直没暴露。**遇到 `List<自定义类型>` 的 JSON 列，把实体字段声明成 `String`，在 Service 里用 Spring 的 `ObjectMapper` 显式读写**（照抄 `Payslip` / `PayslipService.itemsOf()`）。另外 10 个 JSON 列开发时留意这一点

### 代码模式

- **新增基础数据实体照抄四件套**：`entity` + `mapper` + `service`（`extends ServiceImpl`，只需实现 `check()`）+ `controller`（`extends BaseCrudController`，18 行）。实体加 `implements IdEntity`，Lombok 会自动满足接口
- **前端新 CRUD 页面**：写一段 `CrudField[]` 配置 + 塞进 `CrudTable`，不用写表格代码。
  `CrudField.type` 支持 `text / textarea / number / select / multiselect / file`；
  `file` 配 `uploadUrl`（走 `api/request.ts` 的 `upload()` 助手），**该字段不出现在表格列里**；
  `readonly: true` 用于后端自算的字段（例如版本号），弹窗里看得见但改不了
- **`CrudTable` 的 `actions` 插槽**：页面自己的行内按钮塞这儿（作用域参数 `{ row }`），插在「编辑/删除」前面。
  用法见 `views/students/index.vue`（收费开课）和 `views/content/index.vue`（下载）
- **业务校验写在 Service 的 `check()` 里**，Controller 是信任边界，但规则归 Service
- **模块之间不直接查对方的 Mapper**，走对方的 Service（例：`basedata` 要家长账号，加的是 `AuthService.listByRole()`）
- **验收测试不要注入 Service，走 HTTP**。`AcceptanceApi` 就是干这个的。同一 JVM 里直调 Service 会共用 Spring 上下文和事务，"独立对账"这四个字就不成立了 —— 那不是风格问题，是断言的前提
- **`src/test` 里只有 `spring-boot-starter-test` 一个依赖**（test scope）。业务代码依赖没变过，别往 main 里加东西
- **验收造的学生统一用 `AcceptanceResetTest.TEMP_PREFIX` 开头命名**，重置靠这个前缀认人。换成正常名字会导致造的数据清不掉
- **继承了 `BaseCrudController` 就不要改基类那几个方法的签名**。想让列表多带过滤参数，
  得**原签名覆写**（顺带把权限过滤加上，不加就等于绕过）再另开一个新路径 ——
  直接给 `page()` 加参数会变成重载，两个方法映射同一个 URL，Spring 启动就 `Ambiguous mapping`
- **MyBatis-Plus 里 `eq(...).or().eq(...)` 必须用 `and(w -> ...)` 包起来**，
  否则和前面的条件拼出来的优先级是错的（`A AND B OR C` = `(A AND B) OR C`）。
  这个坑在 `ContentAssetService.pageVisible()` 和别的地方都会遇到

### 排课引擎（最容易改错的地方）

- **没有优化阶段，也别去加。** 只有贪心构造，不存在局部搜索/模拟退火/目标函数。计划书里曾写过 `LocalSearchOptimizer` 和 `score = 5×…-2×…-1×…`，那是纸面设计，代码从来没实现，2026-09-16 已从计划书删除。**能排出合法课表就是验收标准**
- **调课校验必须复用 `ConstraintValidator`**，不要另写一套。`ScheduleService.move()` 和 `IncrementalRescheduler` 都走它。另写一套必然出现"自动排的合法、手动挪的非法"
- `IncrementalRescheduler.resolve()` 里对目标时段要做**两次**校验：
  1. 第一次传**空占用集** —— 只查结构性约束（教学网格、教师/学生可用时段、容量、资质）。这些挪开别人也解决不了
  2. 第二次才查占用冲突 —— 这些可以靠挪开别人解决
  **漏掉第一次会让引擎对 08:00 这种非法时段报"可行"**（已修过一次，别再踩）
- 候选时段排序用**负载优先**（`validator.loadOn(date)`），不是"周一到周日"。改回后者课表会全挤在周一周二
- 教师/学生**没配可用时段 = 不受限**。故意如此，否则没配约束数据时一节都排不出来
- 教学时段网格写死在 `TeachingGrid` 常量里（工作日 16:00–21:00，周末 09:00–21:00，30 分钟粒度）

---

## 已知遗留（都是刻意的，不是忘了）

| 项 | 说明 | 何时必须补 |
| --- | --- | --- |
| **教师/学生可用时段没有配置界面** | 数据在库里、种子灌了一批，但用户改不了。排课演示够用 | 有评委问"机构怎么配老师时间"时会露怯，建议批次三前补 |
| **教师/学生不能开登录账号** | 只有种子里的 5 个测试账号能登录，新建的教师学生没有账号 | 演示时要主动说明 |
| **跨周课表一致是巧合不是设计** | 靠"每周输入相同 + 贪心确定性"。锁一节课或调一次课，某些周会漂走 | 方案 B（先解周模式再复制到各周），见下 |
| `/move` 接口无调用方 | 严格模式（撞了就拒绝）。逻辑上被 `/reschedule` 覆盖 | 待定，留着是因为它是真实产品需求 |
| **排课没有优化阶段** | 只有贪心构造，**没有局部搜索、也没有软约束目标函数**。代码里从来没实现过（`GreedyScheduler.java` 注释写明"这一版先不做"），2026-09-16 已从计划书里删掉 —— 别再去做 | 演示话术别说"最优解"，说"能排出合法课表" |
| 无小程序、无短信、无 PPT 导入、无 PDF 导出 | 见 `编码计划书.md` 第一节 | 演示时主动说明是规划中的能力 |

### 排课质量：方案 B 还没做

现在每次都是**整表重排**（`GreedyScheduler`）+ **单点增量**（`IncrementalRescheduler`）。计划书说的"先求解一张周课表模式，再复制到每一周"没做。

后果：跨周一致性靠巧合。一旦有锁定课或手动调课，某些周会偏离。

要做的话是改求解循环的结构 —— 会动到 `IncrementalRescheduler` 的设计，值得先对齐再动。

---

## 验证方式

我（AI）改完代码后通常这样验：

```bash
# 后端
cd backend && mvn -q -B compile           # 编译
mvn -B test                               # 16 个验收对账用例（不用先起后端，自己拉实例）
# 重启（会重置数据库）
PID=$(netstat -ano | grep LISTENING | grep :8080 | awk '{print $NF}'); taskkill //PID $PID //F
mvn -B spring-boot:run

# 前端
cd frontend && npx vite build && rm -rf dist   # 只验能否编译

# 接口（走前端代理，顺便验 proxy + session）
curl -s -c /tmp/cj.txt -X POST http://localhost:5173/api/auth/login \
  -H "Content-Type: application/json" -d '{"username":"principal","password":"123456"}'
```

**硬约束必须独立对账，不能信引擎自报的"0 冲突"。**

### `mvn -B test` —— 验收对账（步骤 12 起，主力）

一致性断言全在这里。**不用先起后端**，测试自己拉一个随机端口的实例。
三个测试类共 25 个用例：

```bash
cd backend && mvn -B test
```

**`AcceptanceReconcileTest`（15 个）** —— 课时 / 资金：

| 用例 | 断言 |
| --- | --- |
| 0 | 排课：`conflictCount == 0` |
| 1 | **验收3** 每个账户 `remaining_hours` == 该账户全部 `lesson_transaction.hours` 之和（全库逐账户） |
| 2 | 账户 `total_hours` / `consumed_hours` == 流水里 `RECHARGE` / `CONSUME` 的合计 |
| 3 | 课时单价 == 已收预收总额 / 已购课时（单价自己重算，不读接口返回的） |
| 4–5 | 收费开课：新学生从无账户到开课；追加充值单价不变 |
| 6 | 一次点名三处联动，且三份账仍然平 |
| 7 | **防重复核销**：同节课同学生再点一次被拒 |
| 8 | **事务回滚**：一批里混进没课时账户的学生，整批不落库 |
| 9–13 | 收费权限 403、家长 / 教师 / 学生的数据范围 |
| 14 | **收尾**：所有写操作跑完，1/3/4 再各验一次 |

**`AcceptanceContentTest`（9 个）** —— 内容托管：

| 用例 | 断言 |
| --- | --- |
| 1 | 上传后从 `/uploads/` **把字节取回来比**，不是看接口返回个文件名就当成功 |
| 2 | 空文件被拒；学生上传被 403 |
| 3 | 归属由后端填，请求体里伪造的 `ownerId` 被无视；版本从 1 起 |
| 4 | 按课程过滤：课程 1 查得到、课程 2 查不到 |
| 5 | 别人的 `PRIVATE` 看不到，自己的看得到 |
| 6 | **`or` 括号**：按课次筛选时自己别课的私藏不混进来（这条拿别人的内容验不出来） |
| 7 | 版本只递增，前端回传的旧版本号不作数 |
| 8 | 改不了 / 删不了别人的；学生建不了内容；自己的能删 |
| 9 | 名称必填；类型和可见范围走白名单（`SUBJECT_GROUP` 被拒） |

**`AcceptanceResetTest`（1 个）** —— 重置本身对不对。

判据一律是**把数据从接口拉下来自己重算**，不看服务自报的"核销成功"。
测试里没有注入任何 Service —— 这是"独立对账"成立的前提，别为了省事改成直调。

细节和"这个测试验过它自己有用"的证据见上面「步骤 12」那一节。

> ⚠️ 种子数据里没有课次，`@BeforeAll` 会先排一次课。所以跑测试会花十几秒。
> 测试跑在自己 JVM 的 H2 里，**不碰你在 :8080 演示的那个库**。

### 已废弃：`充值验收.py`（仓库根目录）

步骤 11 时写的 Python 脚本，断言已全部并进 `AcceptanceReconcileTest` 的用例 4、5、9。
**它比 JUnit 版差在哪**：直接打 `:8080`，会往演示库里塞「充值验收生」，跑完得重启后端才干净。

### 已废弃：`验收对账.py`（**不在仓库里，也从未提交过 git**）

批次三最早那版 Python 对账脚本，已由 `AcceptanceReconcileTest` 取代。
**这份文档曾经引用它，但文件从来不在工作区** —— 以后再看到这个名字，指的就是上面那个 JUnit 类。

### 课表硬约束对账（临时用，Python 一行流）

换 H2 内存库后 `mysql -uroot -p123456 kelifang -e "SELECT ..."` 就失效了 —— 数据活在 JVM 里，不对外开端口。
要临时核一次课表冲突，走 API 拉下来自己算：

```bash
curl -s -b /tmp/cj.txt "http://localhost:8080/api/schedule" | python -c "
import sys, json
from itertools import combinations
rows = json.load(sys.stdin)['data']
overlap = lambda a,b: (a['lessonDate']==b['lessonDate']
                       and a['startTime']<b['endTime'] and b['startTime']<a['endTime'])
for f in ('teacherId','classroomId','classId'):
    print(f, sum(1 for a,b in combinations(rows,2) if a[f] and a[f]==b[f] and overlap(a,b)))
"
# 三个数字都应为 0
```

学生维度要再拉一次班级名单（`/api/basedata/clazz/{id}/student`），两个班有共同学生时时段不能重叠。

**要跑真 SQL** 就临时给 `application.yml` 加一行开 H2 控制台，浏览器打开 `http://localhost:8080/h2-console`（JDBC URL `jdbc:h2:mem:kelifang`，用户 `sa`，密码留空）：

```yaml
spring:
  h2:
    console:
      enabled: true
```

**这行现在没加**（demo 时少一个入口）。需要查库时加上，查完删掉。

### 还没验过的

**浏览器渲染一次都没验过 —— 我起不了浏览器。**
所有页面的实际长相、点击、弹窗，都需要人工打开 http://localhost:5173 确认。已交付但未经人眼验证的页面：

- 登录页 + 五个角色的菜单差异
- 基础数据（5 个 tab）
- 学员管理（学生 + 班级名单穿梭框；**「收费开课」弹窗**只验过接口，弹窗本身没点过）
- 排课与课表（7 列周视图、智能排课弹窗、调课弹窗）
- 签到管理（课次列表、点名弹窗、点名结果弹窗）
- 课时账户（账户表、流水弹窗）
- 工时薪酬（工资单 tab、工时明细 tab、明细弹窗里的「看签到」）
- 财会报表（5 个 tab）
- 经营看板（数字描述列表 + ECharts 折线图）
- **内容托管**（课次下拉、上传、下载按钮；接口经 Vite 代理实测全通，但页面本身没点过）

**新页面验过一次的只有"能不能编译"**：把 `.vue` 丢给 Vite dev server（:5173）按需编译，
没有报 transform 错误，页面调用的接口也经 Vite 代理实测通了。**渲染出来长什么样、弹窗点得动不动，没人看过。**

### 端到端跑法（步骤 13 用过一次，以后照这个来）

起后端（`:8080`）+ Vite（`:5173`），**全程走 `:5173` 代理**，这样 proxy、session cookie、
multipart 上传、`/uploads/**` 静态资源一起验了：

1. 校长登录 → `POST /api/content/upload` 传一个文件 → 记下返回的文件名
2. `GET /uploads/<文件名>` **把字节取回来和原文件比** —— 只看到上传接口返回了文件名不算数
3. `POST /api/content` 建资产，`courseId` 指向某个班的课程
4. 切教师登录 → `GET /api/content/list?courseId=X` → 该资产出现
5. 教师建一条 `PRIVATE` 的 → 切学生登录 → 列表里**不该有**它

> ⚠️ 这个流程会往**演示库**里写数据（内容资产 + 磁盘文件）。跑完重启后端就干净了；
> 磁盘上的文件在 `backend/data/uploads/`，`data/` 已在 `.gitignore` 里，不会进 git。
